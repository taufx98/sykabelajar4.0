import {useCallback,useEffect,useMemo,useRef,useState,type ClipboardEvent,type DragEvent} from 'react';import {ArrowLeft,CheckCircle2,FileText,Headphones,Lock,MessageCircle,Paperclip,Send,ShieldAlert,Star,Trash2,Unlock,X} from 'lucide-react';import {Link,useSearchParams} from 'react-router-dom';import {useApp} from '@/store/AppContext';import {Avatar} from '@/components/ui/Avatar';import {Badge} from '@/components/ui/Badge';import {Button} from '@/components/ui/Button';import {supabase} from '@/lib/supabase';import {blockChatUser,claimChatTicket,closeThread,createTicketThread,getAdminChatHistory,getOrCreateDmThread,hideChatThread,isChatBlocked,loadChatMessagesPage,loadMyThreads,markThreadRead,sendMessage,unblockChatUser,type ChatHistoryRow,type ChatMessage,type ChatThread}from '@/services/chat.service';import {attachChatMedia,isHttpUrl,isImageUrl,uploadChatAttachment,validateChatFile}from '@/services/chatMedia.service';
const PAGE=50,CACHE='syka.chat.v5.';const isTicket=(t:ChatThread)=>t.thread_type==='ticket';const safeUrl=(v:string)=>{try{const u=new URL(v);return/^https?:$/.test(u.protocol)?u.toString():null}catch{return null}};const name=(t:ChatThread,me:string,admin:boolean)=>isTicket(t)?(admin?t.user_name||t.username||'Pengguna':'Admin Sykabelajar'):(t.user_id===me?t.other_user_name||t.other_username||'Pengguna':t.user_name||t.username||'Pengguna');const uname=(t:ChatThread,me:string,admin:boolean)=>isTicket(t)?(admin?t.username||'':'sykabelajar'):(t.user_id===me?t.other_username||'':t.username||'');const avatar=(t:ChatThread,me:string,admin:boolean)=>isTicket(t)?(admin?t.avatar_url:undefined):(t.user_id===me?t.other_avatar_url:t.avatar_url);function cacheGet(id:string):ChatMessage[]{try{const x=JSON.parse(localStorage.getItem(CACHE+id)||'[]');return Array.isArray(x)?x:[]}catch{return[]}}function cacheSet(id:string,x:ChatMessage[]){try{localStorage.setItem(CACHE+id,JSON.stringify(x.slice(-200)))}catch{}}
function Modal({children,onClose}:{children:React.ReactNode;onClose:()=>void}){return <div className="fixed inset-0 z-[150] flex items-center justify-center p-4"><button className="absolute inset-0 bg-black/70" onClick={onClose}/><div className="relative w-full max-w-lg card p-5">{children}</div></div>}
function History({rows,onClose,onView}:{rows:ChatHistoryRow[];onClose:()=>void;onView:(r:ChatHistoryRow)=>void}){const[q,setQ]=useState(''),[rate,setRate]=useState(''),[handler,setHandler]=useState(''),[desc,setDesc]=useState(true);const handlers=Array.from(new Map(rows.filter(x=>x.handled_by).map(x=>[x.handled_by!,x.handled_name||x.handled_username||'Petugas'])).entries());const list=rows.filter(x=>`${x.user_name} ${x.username||''}`.toLowerCase().includes(q.toLowerCase())&&(!rate||String(x.rating)===rate)&&(!handler||x.handled_by===handler)).sort((a,b)=>desc?b.created_at.localeCompare(a.created_at):a.created_at.localeCompare(b.created_at));return <Modal onClose={onClose}><div className="flex items-center justify-between mb-4"><div><h2 className="font-bold text-fg">History Chat & Rating</h2><p className="text-xs text-fg-muted">Riwayat ticket dan penilaian.</p></div><button onClick={onClose}><X size={18}/></button></div><div className="grid md:grid-cols-4 gap-2 mb-4"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Nama / username"/><select className="input" value={rate} onChange={e=>setRate(e.target.value)}><option value="">Semua rating</option>{[1,2,3,4,5].map(n=><option key={n} value={n}>★ {n}</option>)}</select><select className="input" value={handler} onChange={e=>setHandler(e.target.value)}><option value="">Semua petugas</option>{handlers.map(([id,n])=><option key={id} value={id}>{n}</option>)}</select><Button variant="outline" onClick={()=>setDesc(v=>!v)}>{desc?'Terbaru':'Terlama'}</Button></div><div className="max-h-[60vh] overflow-y-auto space-y-2">{list.map(r=><div key={r.thread_id} className="p-3 rounded-xl border surface-border flex items-center gap-3"><div className="flex-1 min-w-0"><p className="text-sm font-semibold text-fg truncate">{r.user_name}</p><p className="text-[11px] text-fg-muted truncate">@{r.username||'—'} · {r.subject||'Tanpa subjek'}</p><p className="text-[10px] text-fg-muted">{new Date(r.created_at).toLocaleString('id-ID')}{r.handled_name?` · ${r.handled_name}`:''}</p></div>{r.rating&&<div className="flex">{[1,2,3,4,5].map(n=><Star key={n} size={12} className={n<=r.rating!?'text-amber-400 fill-amber-400':'text-slate-700'}/>)}</div>}<Button size="sm" variant="outline" onClick={()=>onView(r)}>View Chat</Button></div>)}{!list.length&&<p className="py-8 text-center text-xs text-fg-muted">History tidak ditemukan.</p>}</div></Modal>}
export function MessagesPageV5(){const{user,toast}=useApp();const me=user?.id||'',admin=user?.role==='admin';const[params,setParams]=useSearchParams();const[threads,setThreads]=useState<ChatThread[]>([]);const[selected,setSelected]=useState<ChatThread|null>(null);const[messages,setMessages]=useState<ChatMessage[]>([]);const[input,setInput]=useState('');const[loading,setLoading]=useState(true);const[sending,setSending]=useState(false);const[fileBusy,setFileBusy]=useState(false);const[mobileList,setMobileList]=useState(true);const[blocked,setBlocked]=useState(false);const[blockedByMe,setBlockedByMe]=useState(false);const[ticketModal,setTicketModal]=useState(false);const[ticketBusy,setTicketBusy]=useState(false);const[ratingModal,setRatingModal]=useState(false);const[ratingBusy,setRatingBusy]=useState(false);const[preview,setPreview]=useState<string|null>(null);const[drop,setDrop]=useState(false);const[older,setOlder]=useState(true);const[loadingOlder,setLoadingOlder]=useState(false);const[history,setHistory]=useState<ChatHistoryRow[]|null>(null);const[fileInput]=useState(()=>({current:null as HTMLInputElement|null}));const[box]=useState(()=>({current:null as HTMLDivElement|null}));
const reload=useCallback(async()=>{if(!user)return;try{setThreads(await loadMyThreads())}catch(e:any){toast(e?.message||'Gagal memuat chat.','error')}finally{setLoading(false)}},[user?.id,toast]);useEffect(()=>{void reload();const i=setInterval(()=>void reload(),30000);return()=>clearInterval(i)},[reload]);useEffect(()=>{const id=params.get('user_id');if(!id||!user)return;let live=true;(async()=>{try{const t=await getOrCreateDmThread(id);if(live){setSelected(t);setMobileList(false);setThreads(x=>[t,...x.filter(y=>y.id!==t.id)])}}catch(e:any){if(live)toast(e?.message||'Chat tidak tersedia.','error')}finally{if(live){const p=new URLSearchParams(params);p.delete('user_id');setParams(p,{replace:true})}}})();return()=>{live=false}},[params,user?.id,setParams,toast]);useEffect(()=>{if(!selected)return;let live=true;const c=cacheGet(selected.id);if(c.length){setMessages(c);setOlder(c.length>=PAGE)}(async()=>{try{const latest=await loadChatMessagesPage(selected.id,PAGE);if(live){setMessages(latest);cacheSet(selected.id,latest);setOlder(latest.length>=PAGE);requestAnimationFrame(()=>{if(box.current)box.current.scrollTop=box.current.scrollHeight})}await markThreadRead(selected.id)}catch(e:any){if(live&&c.length===0)toast(e?.message||'Gagal memuat pesan.','error')}})();const ch=supabase.channel(`chat-v5-${selected.id}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'chat_messages',filter:`thread_id=eq.${selected.id}`},p=>{const m=p.new as ChatMessage;setMessages(cur=>cur.some(x=>x.id===m.id)?cur:[...cur,m]);if(m.sender_id!==me)void markThreadRead(selected.id);requestAnimationFrame(()=>{if(box.current)box.current.scrollTop=box.current.scrollHeight});void reload()}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'chat_threads',filter:`id=eq.${selected.id}`},p=>setSelected(p.new as ChatThread)).subscribe();return()=>{live=false;void supabase.removeChannel(ch)}},[selected?.id,me,reload,toast]);const counterpart=useMemo(()=>selected?(selected.user_id===me?selected.participant_id:selected.user_id):null,[selected,me]);useEffect(()=>{if(!counterpart)return;let live=true;(async()=>{try{const b=await isChatBlocked(counterpart);if(live)setBlocked(b);const{data}=await supabase.from('chat_blocks').select('blocker_id').eq('blocker_id',me).eq('blocked_id',counterpart).maybeSingle();if(live)setBlockedByMe(Boolean(data))}catch{}})();return()=>{live=false}},[counterpart,me]);useEffect(()=>{const el=box.current;if(!el||!selected)return;const onScroll=()=>{if(el.scrollTop>80||loadingOlder||!older||!messages.length)return;setLoadingOlder(true);const before=messages[0].created_at,h=el.scrollHeight;void loadChatMessagesPage(selected.id,PAGE,before).then(v=>{if(!v.length){setOlder(false);return}setMessages(cur=>{const n=[...v,...cur.filter(x=>!v.some(y=>y.id===x.id))];cacheSet(selected.id,n);return n});requestAnimationFrame(()=>{el.scrollTop=el.scrollHeight-h})}).catch(e=>toast(e?.message||'Gagal memuat riwayat lama.','error')).finally(()=>setLoadingOlder(false))};el.addEventListener('scroll',onScroll);return()=>el.removeEventListener('scroll',onScroll)},[selected?.id,messages,older,loadingOlder,toast]);
const open=threads.filter(t=>t.status==='open'),closed=threads.filter(t=>t.status==='closed'),activeTicket=!admin?open.find(isTicket):null;const choose=async(t:ChatThread)=>{if(selected?.id===t.id)return;setSelected(t);setMessages([]);setMobileList(false);await markThreadRead(t.id).catch(()=>{})};const send=async()=>{if(!selected||selected.status!=='open'||blocked||!input.trim()||sending||(admin&&isTicket(selected)&&!selected.handled_by))return;const text=input.trim(),opt:ChatMessage={id:`local-${crypto.randomUUID()}`,thread_id:selected.id,sender_id:me,body:text,created_at:new Date().toISOString()};setMessages(v=>[...v,opt]);setInput('');setSending(true);try{const saved=await sendMessage(selected.id,text);setMessages(v=>v.map(x=>x.id===opt.id?saved:x));cacheSet(selected.id,[...messages,saved])}catch(e:any){setMessages(v=>v.filter(x=>x.id!==opt.id));toast(e?.message||'Gagal mengirim pesan.','error')}finally{setSending(false)}};const sendFile=async(f:File)=>{if(!selected||selected.status!=='open'||blocked||fileBusy)return;try{validateChatFile(f);setFileBusy(true);const u=await uploadChatAttachment(f,selected.id,me);const saved=await sendMessage(selected.id,u.secure_url);await attachChatMedia(u.media_id,saved.id);setMessages(v=>[...v,saved]);toast('Lampiran berhasil dikirim.','success')}catch(e:any){toast(e?.message||'Lampiran gagal dikirim.','error')}finally{setFileBusy(false)}};const hide=async(t:ChatThread)=>{setThreads(v=>v.filter(x=>x.id!==t.id));if(selected?.id===t.id){setSelected(null);setMessages([]);setMobileList(true)}try{await hideChatThread(t.id);toast('Chat telah dihapus','success')}catch(e:any){toast(e?.message||'Gagal menghapus chat.','error');void reload()}};const block=async()=>{if(!counterpart)return;const old=blocked;setBlocked(!old);try{if(old)await unblockChatUser(counterpart);else await blockChatUser(counterpart);toast(old?'Blokir chat dibuka.':'Pengguna diblokir.','success')}catch(e:any){setBlocked(old);toast(e?.message||'Gagal mengubah blokir.','error')}};const ticket=async(s:string,d:string)=>{setTicketBusy(true);try{const t=await createTicketThread(s,d);setTicketModal(false);setSelected(t);setMobileList(false);setThreads(v=>[t,...v.filter(x=>x.id!==t.id)]);toast('Laporan berhasil dikirim.','success')}catch(e:any){toast(e?.message||'Gagal membuat laporan.','error')}finally{setTicketBusy(false)}};const claim=async()=>{if(!selected)return;setTicketBusy(true);try{const t=await claimChatTicket(selected.id);setSelected(t);setThreads(v=>v.map(x=>x.id===t.id?t:x));toast('Ticket berhasil diambil alih.','success')}catch(e:any){toast(e?.message||'Ticket gagal diambil alih.','error')}finally{setTicketBusy(false)}};const finish=async()=>{if(!selected)return;setTicketBusy(true);try{const t=await closeThread(selected.id);setSelected(t);setThreads(v=>v.map(x=>x.id===t.id?t:x));toast('Percakapan diselesaikan.','success')}catch(e:any){toast(e?.message||'Gagal menyelesaikan ticket.','error')}finally{setTicketBusy(false)}};const rate=async(n:number)=>{if(!selected)return;setRatingBusy(true);try{const t=await closeThread(selected.id,n);setSelected(t);setThreads(v=>v.map(x=>x.id===t.id?t:x));setRatingModal(false);toast('Rating tersimpan.','success')}catch(e:any){toast(e?.message||'Gagal menyimpan rating.','error')}finally{setRatingBusy(false)}};const loadHistory=async()=>{try{setHistory(await getAdminChatHistory({limit:200}))}catch(e:any){toast(e?.message||'Gagal memuat history chat.','error')}};const paste=(e:ClipboardEvent)=>{const i=Array.from(e.clipboardData.items).find(x=>x.kind==='file'&&x.type.startsWith('image/'));const f=i?.getAsFile();if(f){e.preventDefault();void sendFile(f)}};const dropFile=(e:DragEvent)=>{e.preventDefault();setDrop(false);const f=e.dataTransfer.files?.[0];if(f)void sendFile(f)};
return <div className="h-[calc(100vh-56px)] md:h-screen overflow-hidden surface-bg p-2 md:p-4"><div className="h-full max-w-7xl mx-auto flex flex-col"><div className="shrink-0 flex items-center justify-between pb-2"><Link to="/home" className="inline-flex items-center gap-2 text-xs text-fg-muted"><ArrowLeft size={14}/>Kembali</Link>{!admin&&<Button size="sm" icon={<Headphones size={14}/>} onClick={()=>activeTicket?toast('Anda masih memiliki laporan yang sedang berjalan. Tunggu hingga laporan diselesaikan oleh Admin.','info'):setTicketModal(true)}>Hubungi Admin</Button>}</div><div className="shrink-0 flex items-center gap-3 pb-3"><MessageCircle size={21} className="text-accent"/><h1 className="text-xl font-bold text-fg">Pesan</h1><Badge color="moss">{open.length} aktif</Badge>{admin&&<Button size="sm" variant="outline" className="ml-auto" onClick={()=>{setHistory([]);void loadHistory()}}>History Chat & Rating</Button>}</div><div className="flex-1 min-h-0 grid md:grid-cols-[320px_1fr] rounded-2xl overflow-hidden border surface-border surface-card-bg"><aside className={`${mobileList?'flex':'hidden'} md:flex flex-col min-h-0 border-r surface-border`}><div className="p-3 border-b surface-border"><p className="text-xs font-semibold text-fg">Percakapan</p><p className="text-[10px] text-fg-muted">DM antar pengguna & Helpdesk Admin</p></div><div className="flex-1 overflow-y-auto p-2">{loading?<p className="py-8 text-center text-xs text-fg-muted">Memuat…</p>:<>{open.map(t=><ThreadRow key={t.id} t={t} me={me} admin={admin} selected={selected?.id===t.id} onClick={()=>void choose(t)} onHide={()=>void hide(t)}/>) }{closed.length>0&&<><p className="px-2 pt-4 pb-2 text-[10px] uppercase text-fg-muted">Selesai</p>{closed.map(t=><ThreadRow key={t.id} t={t} me={me} admin={admin} selected={selected?.id===t.id} onClick={()=>void choose(t)} onHide={()=>void hide(t)} closed/>)}</>}</>}</div></aside><section className={`${mobileList?'hidden':'flex'} md:flex flex-col min-h-0 min-w-0`}>{!selected?<div className="flex-1 flex items-center justify-center"><p className="text-sm text-fg-muted">Pilih percakapan.</p></div>:<><div className="shrink-0 px-4 py-3 border-b surface-border flex items-center gap-3"><button className="md:hidden" onClick={()=>setMobileList(true)}><ArrowLeft size={17}/></button><Avatar name={name(selected,me,admin)} id={counterpart||selected.user_id} size={40} src={avatar(selected,me,admin)}/><div className="min-w-0 flex-1"><Link to={uname(selected,me,admin)?`/profile/@${uname(selected,me,admin)}`:'/home'} className="font-semibold text-sm text-fg hover:text-accent truncate block">{name(selected,me,admin)}</Link><p className="text-[10px] text-fg-muted truncate">@{uname(selected,me,admin)}{isTicket(selected)?' · Helpdesk Admin':''}</p></div>{!isTicket(selected)&&counterpart&&<Button size="sm" variant="outline" onClick={()=>void block()} icon={blocked?<Unlock size={14}/>:<Lock size={14}/>}>{blocked?'Buka Blokir':'Blokir'}</Button>}{admin&&isTicket(selected)&&selected.status==='open'&&!selected.handled_by&&<Button size="sm" variant="outline" onClick={()=>void claim()} disabled={ticketBusy}>Jawab Ticket</Button>}{admin&&isTicket(selected)&&selected.status==='open'&&<Button size="sm" variant="outline" onClick={()=>void finish()} disabled={ticketBusy}>Tandai Selesai</Button>}{!admin&&isTicket(selected)&&selected.status==='closed'&&!selected.rating&&<Button size="sm" variant="outline" onClick={()=>setRatingModal(true)}>Beri Rating</Button>}</div><div className="shrink-0 px-4 py-2 bg-amber-500/10 border-b border-amber-500/20 flex gap-2"><ShieldAlert size={14} className="text-amber-400"/><p className="text-[10px] text-amber-300 leading-relaxed">🔒 Himbauan Keamanan: Jangan membagikan data pribadi sensitif (password, nomor HP, atau data finansial). Waspadai penipuan mengatasnamakan platform.</p></div>{isTicket(selected)&&<div className="shrink-0 px-4 py-2 border-b surface-border"><p className="text-[9px] uppercase text-fg-muted">Ringkasan Laporan</p><p className="text-sm font-medium text-fg">{selected.subject||'Bantuan'}</p>{selected.description&&<p className="text-xs text-fg-muted mt-1">{selected.description}</p>}</div>}<div ref={el=>{box.current=el}} onDragEnter={e=>{e.preventDefault();setDrop(true)}} onDragOver={e=>e.preventDefault()} onDragLeave={e=>{if(e.currentTarget===e.target)setDrop(false)}} onDrop={dropFile} className={`relative flex-1 min-h-0 overflow-y-auto p-4 space-y-3 ${drop?'ring-2 ring-accent/40 bg-accent/5':''}`}>{loadingOlder&&<p className="text-center text-[10px] text-fg-muted">Memuat riwayat lama…</p>}{messages.map(m=><div key={m.id} className={`flex ${m.sender_id===me?'justify-end':'justify-start'}`}><div className={`max-w-[80%] px-3.5 py-2.5 rounded-2xl ${m.sender_id===me?'bg-moss-600 text-white rounded-br-sm':'surface-elevated border surface-border text-fg rounded-bl-sm'}`}>{renderMessage(m.body,setPreview)}<p className="text-[9px] mt-1 opacity-65">{new Date(m.created_at).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})}</p></div></div>)}</div><div className="shrink-0 p-3 border-t surface-border">{blocked?<div className="rounded-xl border border-red-500/20 bg-red-500/5 p-3 text-center text-sm text-red-300">{blockedByMe?'Anda memblokir pengguna ini.':`${name(selected,me,admin)} telah memblokir anda`}</div>:selected.status!=='open'?<div className="p-3 text-center text-xs text-fg-muted">Percakapan telah selesai.</div>:admin&&isTicket(selected)&&!selected.handled_by?<div className="p-3 rounded-xl border border-amber-500/20 bg-amber-500/5 text-center"><p className="text-sm text-amber-300">Ambil alih ticket terlebih dahulu untuk membalas.</p><Button size="sm" className="mt-2" onClick={()=>void claim()}>Jawab Ticket</Button></div>:<div className="flex items-end gap-2" onPaste={paste}><input ref={el=>{fileInput.current=el}} type="file" accept=".jpg,.jpeg,.png,.pdf" className="hidden" onChange={e=>{const f=e.target.files?.[0];if(f)void sendFile(f);e.currentTarget.value=''}}/><Button size="sm" variant="outline" disabled={fileBusy||sending} onClick={()=>fileInput.current?.click()} icon={<Paperclip size={15}/>}/><textarea value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();void send()}}} placeholder="Tulis pesan…" rows={1} className="input flex-1 resize-none max-h-28"/><Button size="sm" disabled={sending||!input.trim()} onClick={()=>void send()} icon={<Send size={15}/>}/></div>}</div></>}</section></div></div>{ticketModal&&<TicketModal close={()=>setTicketModal(false)} submit={ticket} busy={ticketBusy}/>} {ratingModal&&<RatingModal close={()=>setRatingModal(false)} submit={rate} busy={ratingBusy}/>} {preview&&<PreviewModal url={preview} close={()=>setPreview(null)}/>} {history&&<History rows={history} onClose={()=>setHistory(null)} onView={async r=>{setHistory(null);const t=threads.find(x=>x.id===r.thread_id)||({id:r.thread_id,user_id:r.user_id,participant_id:me,status:r.status==='closed'?'closed':'open',rating:r.rating,closed_at:r.closed_at,created_at:r.created_at,thread_type:'ticket',subject:r.subject,description:r.description,user_name:r.user_name,username:r.username||undefined,handled_by:r.handled_by,handled_at:null} as ChatThread);setSelected(t);setMobileList(false);try{setMessages(await loadChatMessagesPage(r.thread_id,PAGE))}catch(e:any){toast(e?.message||'Gagal membuka history.','error')}}}/>}</div>}
function TicketModal({close,submit,busy}:{close:()=>void;submit:(s:string,d:string)=>void;busy:boolean}){const[s,setS]=useState(''),[d,setD]=useState('');return <Modal close={close} onClose={close}><h3 className="font-semibold text-fg">Hubungi Admin</h3><input className="input w-full mt-4" value={s} onChange={e=>setS(e.target.value)} placeholder="Subjek kendala *"/><textarea className="input w-full mt-3 min-h-32" value={d} onChange={e=>setD(e.target.value)} placeholder="Deskripsi kendala *"/><div className="flex justify-end gap-2 mt-4"><Button variant="ghost" onClick={close}>Batal</Button><Button disabled={busy||!s.trim()||!d.trim()} onClick={()=>submit(s.trim(),d.trim())}>{busy?'Mengirim…':'Kirim Laporan'}</Button></div></Modal>}
function RatingModal({close,submit,busy}:{close:()=>void;submit:(n:number)=>void;busy:boolean}){const[r,setR]=useState(0);return <Modal onClose={close}><h3 className="font-semibold text-fg text-center">Nilai Layanan Admin</h3><div className="flex justify-center gap-2 my-5">{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setR(n)}><Star size={30} className={n<=r?'text-amber-400 fill-amber-400':'text-slate-600'}/></button>)}</div><Button fullWidth disabled={!r||busy} onClick={()=>submit(r)}>{busy?'Menyimpan…':'Kirim Rating'}</Button></Modal>}
function PreviewModal({url,close}:{url:string;close:()=>void}){return <div className="fixed inset-0 z-[160] bg-black/85 flex items-center justify-center p-4"><button className="absolute inset-0" onClick={close}/><div className="relative max-w-[92vw] max-h-[90vh] flex flex-col items-center gap-3"><button onClick={close} className="absolute -top-10 right-0 text-white"><X size={22}/></button><img src={url} alt="Pratinjau media" className="max-w-[92vw] max-h-[80vh] rounded-xl object-contain"/><a href={url} target="_blank" rel="noopener noreferrer" className="text-xs text-white underline">Buka sumber asli</a></div></div>}
