import {useEffect,useState} from 'react';import {Trophy,ChevronDown} from 'lucide-react';import {Card} from '@/components/ui/Card';import {Avatar} from '@/components/ui/Avatar';import {supabase} from '@/lib/supabase';
const classes:any={SD:['1','2','3','4','5','6'],SMP:['7','8','9'],SMA:['10','11','12']};
export function LeaderboardPageV2(){const[r,setR]=useState<any[]>([]),[g,setG]=useState('ALL'),[c,setC]=useState('');useEffect(()=>{supabase.rpc('get_public_leaderboard',{p_limit:1000}).then(x=>setR(x.data||[]))},[]);const f=g==='ALL'?r:r.filter(x=>new RegExp(g==='SMA'?'sma|alumni':g,'i').test(x.grade||''));const top=f.slice(0,3),rest=f.slice(3);return <div className="p-4 md:p-6"><div className="max-w-5xl mx-auto"><header className="text-center mb-6"><Trophy className="mx-auto text-amber-400"/><h1 className="font-display text-2xl font-bold text-fg mt-2">Papan Peringkat</h1></header><div className="flex justify-center gap-2 flex-wrap mb-6">{['ALL','SD','SMP','SMA'].map(x=><button key={x} onClick={()=>{setG(x);setC('')}} className={`px-4 py-2 rounded-xl text-sm ${g===x?'bg-accent text-white':'surface-elevated text-fg-muted'}`}>{x==='ALL'?'Semua':x}</button>)}{g!=='ALL'&&<div className="relative"><select value={c} onChange={e=>setC(e.target.value)} className="input appearance-none pr-9"><option value="">Semua {g}</option>{classes[g].map((x:string)=><option key={x} value={x}>Kelas {x}</option>)}</select><ChevronDown className="absolute right-3 top-1/2 -translate-y-1/2 pointer-events-none" size={15}/></div>}</div><div className="grid grid-cols-1 md:grid-cols-3 gap-4 max-w-3xl mx-auto mb-6">{top.map((x,i)=><Card key={x.user_id} className="p-5 text-center"><Avatar name={x.display_name||x.username} id={x.user_id} size={i===0?82:68} src={x.avatar_url||undefined} ring/><p className="font-semibold text-fg mt-3">{x.display_name||x.username}</p><p className="text-xs text-fg-muted">@{x.username}</p><p className="text-xl font-bold text-accent mt-2">{Number(x.xp||0).toLocaleString('id-ID')} XP</p><span className="text-xs text-fg-muted">#{i+1}</span></Card>)}</div><div className="space-y-2 max-w-3xl mx-auto">{rest.map((x,i)=><Card key={x.user_id} className="p-3 flex items-center gap-3"><span className="w-7 text-center text-xs text-fg-muted">#{i+4}</span><Avatar name={x.display_name||x.username} id={x.user_id} size={40} src={x.avatar_url||undefined}/><div className="flex-1 min-w-0"><p className="text-sm font-semibold text-fg truncate">{x.display_name||x.username}</p><p className="text-xs text-fg-muted">@{x.username}</p></div><b className="text-sm text-fg">{Number(x.xp||0).toLocaleString('id-ID')} XP</b></Card>)}</div></div></div>}
