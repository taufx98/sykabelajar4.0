import {useEffect,useMemo,useState} from 'react';import {CalendarCheck,Check,Clock,Flame,Award,Loader2,Sparkles,Info} from 'lucide-react';import {Card} from '@/components/ui/Card';import {Button} from '@/components/ui/Button';import {Badge} from '@/components/ui/Badge';import {useApp} from '@/store/AppContext';import {claimAndCompleteDailyTask,getDailyTasks,type LiveDailyTask} from '@/services/daily-task.service';import {supabase} from '@/lib/supabase';
export function DailyTasksPage(){const{toast}=useApp();const[tasks,setTasks]=useState<LiveDailyTask[]>([]);const[loading,setLoading]=useState(true);const[busyId,setBusyId]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);const[checkin,setCheckin]=useState<any>(null);const[checked,setChecked]=useState(false);const[eventOpen,setEventOpen]=useState(false);const today=new Date().toLocaleDateString('id-ID',{weekday:'long',day:'numeric',month:'long'});
 const load=async()=>{setLoading(true);setError(null);try{setTasks(await getDailyTasks());const d=await supabase.from('daily_checkins').select('*').eq('checkin_date',new Date().toISOString().slice(0,10)).maybeSingle();if(d.error)throw d.error;setChecked(Boolean(d.data));const r=await supabase.rpc('resolve_daily_checkin_reward',{p_date:new Date().toISOString().slice(0,10),p_day_number:1});if(r.error)throw r.error;setCheckin(r.data||null)}catch(e:any){setError(e?.message??'Daily Tasks gagal dimuat.');setTasks([])}finally{setLoading(false)}};
 useEffect(()=>{void load()},[]);const completedCount=useMemo(()=>tasks.filter(t=>t.completed).length,[tasks]);const totalXp=useMemo(()=>tasks.filter(t=>t.completed).reduce((s,t)=>s+t.exp,0),[tasks]);const claimCheckin=async()=>{try{const r=await supabase.rpc('claim_daily_checkin',{p_date:new Date().toISOString().slice(0,10),p_day_number:1});if(r.error)throw r.error;setChecked(true);toast(`Check-in berhasil. +${Number(r.data?.reward_amount||0)} ${r.data?.reward_type||'XP'}.`,'success');await load()}catch(e:any){toast(e.message||'Check-in gagal.','error')}};const claimTask=async(id:string)=>{try{const r:any=await claimAndCompleteDailyTask(id);toast(`Task selesai. +${Number(r?.exp??0)} XP dan +${Number(r?.points??0)} Edu Coin.`,'success');await load()}catch(e:any){toast(e?.message??'Task gagal diselesaikan.','error')}};
 return <div><div className="sticky top-0 z-20 glass border-b surface-border px-4 py-3"><h2 className="font-display font-bold text-lg text-fg">Daily Tasks & Check-in</h2><p className="text-xs text-slate-500">{today} · data live dari Supabase</p></div><div className="p-4 space-y-4"><Card className={`p-4 border ${checkin?.event_name?'border-accent/40 ring-1 ring-accent/10':''}`}><div className="flex items-start gap-4">{checkin?.icon_url?<img src={checkin.icon_url} alt="Ikon event" className="w-14 h-14 rounded-2xl object-contain surface-elevated"/>:<div className="w-14 h-14 rounded-2xl bg-amber-500/20 flex items-center justify-center"><CalendarCheck size={28} className="text-amber-400"/></div>}<div className="flex-1"><div className="flex items-center gap-2"><p className="font-semibold text-fg">Check-in Harian</p>{checkin?.event_name&&<Badge color="moss"><Sparkles size={10}/> {checkin.event_name}</Badge>}</div><p className="text-xs text-fg-muted mt-1">{checkin?.event_description||`Hadiah reguler: +${Number(checkin?.regular_xp||50)} XP & +${Number(checkin?.regular_coin||10)} Coin.`}</p><div className="mt-2 flex gap-2 flex-wrap"><span className="text-xs text-accent font-semibold">+{Number(checkin?.reward_amount||checkin?.regular_xp||50)} {checkin?.reward_type||'XP'}</span>{Number(checkin?.coin_amount||0)>0&&<span className="text-xs text-amber-300 font-semibold">+{Number(checkin.coin_amount)} Coin</span>}</div></div><div className="flex items-center gap-1">{checkin?.event_name&&<button className="w-7 h-7 rounded-full border surface-border text-xs text-fg-muted" onClick={()=>setEventOpen(true)} aria-label="Info event"><Info size={14}/></button>}<Button size="sm" disabled={checked} loading={busyId==='checkin'} onClick={()=>{setBusyId('checkin');void claimCheckin().finally(()=>setBusyId(null))}}>{checked?'Sudah Check-in':'Check-in'}</Button></div></div>{checkin?.event_name&&<div className={`mt-3 rounded-xl p-2 text-[11px] ${checkin.frame_style==='red-white'?'border border-red-400/30':'border border-accent/20'} ${checkin.sparkle?'animate-pulse':''}`}>Tema event aktif hari ini.</div>}</Card><Card className="p-4 bg-gradient-to-r from-amber-500/15 to-transparent border-amber-500/20"><div className="flex items-center gap-4"><div className="w-14 h-14 rounded-2xl bg-amber-500/20 flex items-center justify-center"><Flame size={28} className="text-amber-400"/></div><div className="flex-1"><p className="text-sm font-semibold text-fg">Aktivitas Harian</p><p className="text-xs text-slate-400">Streak dan progres mengikuti catatan backend.</p></div><div className="text-right"><p className="text-2xl font-bold text-fg">{completedCount}</p><p className="text-[10px] text-slate-500">task selesai</p></div></div></Card><div className="grid grid-cols-2 gap-3"><Card className="p-4"><p className="text-xs text-slate-500 mb-1">Task Selesai</p><p className="text-2xl font-bold text-fg">{completedCount}/{tasks.length}</p></Card><Card className="p-4"><p className="text-xs text-slate-500 mb-1">XP Hari Ini</p><p className="text-2xl font-bold gradient-text">+{totalXp}</p></Card></div>{loading&&<Card className="p-8 text-center text-sm text-slate-500"><Loader2 size={18} className="animate-spin mx-auto mb-2"/>Memuat task dari backend...</Card>}{error&&!loading&&<Card className="p-8 text-center text-sm text-red-300">{error}</Card>}{!loading&&!error&&!tasks.length&&<Card className="p-8 text-center text-sm text-slate-500">Belum ada Daily Task aktif di backend.</Card>}{!loading&&!error&&tasks.map(task=>{const expires=task.endsAt?new Date(task.endsAt):null;return <Card key={task.id} className={`p-4 ${task.completed?'opacity-60':''}`}><div className="flex items-start gap-3"><div className="w-10 h-10 rounded-xl surface-elevated flex items-center justify-center shrink-0"><CalendarCheck size={18} className="text-accent"/></div><div className="flex-1 min-w-0"><div className="flex items-center gap-2 mb-1"><h3 className="font-semibold text-sm text-fg">{task.title}</h3>{task.completed&&<Badge color="moss"><Check size={10}/> Selesai</Badge>}</div><p className="text-xs text-slate-400 mb-2">{task.description||'Aktivitas harian SykaBelajar.'}</p><div className="flex items-center gap-3 flex-wrap"><span className="text-xs text-accent font-semibold">+{task.exp} XP</span><span className="text-xs text-amber-300 font-semibold">+{task.points} Coin</span>{expires&&<span className="text-xs text-slate-500 flex items-center gap-1"><Clock size={11}/> Sampai {expires.toLocaleString('id-ID')}</span>}</div></div></div>{!task.completed&&<div className="mt-3 pl-[52px]"><Button size="sm" loading={busyId===task.id} disabled={busyId!==null} onClick={()=>{setBusyId(task.id);void claimTask(task.id).finally(()=>setBusyId(null))}} icon={<Award size={14}/>}>Selesaikan & Klaim</Button></div>}</Card>})}{eventOpen&&<div className="fixed inset-0 z-[100] bg-black/70 flex items-center justify-center p-4"><Card className="w-full max-w-sm p-5"><div className="flex justify-between"><h3 className="font-semibold text-fg">{checkin?.event_name}</h3><button onClick={()=>setEventOpen(false)}>×</button></div><p className="text-sm text-fg-muted mt-3">{checkin?.event_description||'Event khusus check-in.'}</p><Button className="mt-4" fullWidth onClick={()=>setEventOpen(false)}>Tutup</Button></Card></div>}</div></div>}
