import { supabase } from '@/lib/supabase';
import { emitSykaEvent } from '@/lib/realtimeBus';
export type ChatThreadType='dm'|'ticket'|string;
export interface ChatThread{id:string;user_id:string;participant_id?:string|null;status:'open'|'closed';rating:number|null;closed_at:string|null;created_at:string;thread_type?:ChatThreadType;subject?:string|null;description?:string|null;user_name?:string;username?:string;avatar_url?:string;other_user_name?:string;other_username?:string;other_avatar_url?:string;last_message?:string;last_message_at?:string;unread_count?:number;}
export interface ChatMessage{id:string;thread_id:string;sender_id:string;body:string;created_at:string;}
export type FollowStatus='none'|'pending'|'approved'|'auto';
function mapRpcError(error:any):Error{const msg=String(error?.message??error??'Terjadi kesalahan.');const known:Record<string,string>={ACTIVE_TICKET_EXISTS:'Anda masih memiliki laporan yang sedang berjalan. Tunggu hingga laporan diselesaikan oleh Admin.',ADMIN_UNAVAILABLE:'Admin sedang tidak tersedia. Silakan coba lagi nanti.',FOLLOW_REQUIRED:'Anda harus mengikuti pengguna tersebut dan menunggu persetujuan sebelum dapat mengirim pesan.',MESSAGES_NOT_ACCEPTED:'Pengguna tersebut tidak menerima pesan.',THREAD_CLOSED:'Percakapan ini sudah ditutup.',ACCESS_DENIED:'Anda tidak memiliki akses ke percakapan ini.',INVALID_RECIPIENT:'Penerima pesan tidak valid.',USER_NOT_FOUND:'Pengguna tidak ditemukan.',MESSAGE_REQUIRED:'Pesan tidak boleh kosong.',CHAT_BLOCKED:'Anda tidak dapat mengirim pesan karena hubungan chat diblokir.'};const key=Object.keys(known).find(k=>msg.includes(k));return new Error(key?known[key]:msg);}
export async function getOrCreateDmThread(otherUserId:string){const{data,error}=await supabase.rpc('get_or_create_dm_thread',{p_other_user_id:otherUserId});if(error)throw mapRpcError(error);return data as ChatThread;}
export async function loadMyThreads(){const{data,error}=await supabase.rpc('load_my_threads');if(error)throw error;return(data??[])as ChatThread[];}
export async function sendMessage(threadId:string,body:string){const text=body.trim();if(!text)throw new Error('Pesan tidak boleh kosong.');const{data,error}=await supabase.rpc('send_chat_message',{p_thread_id:threadId,p_body:text});if(error)throw mapRpcError(error);return data as ChatMessage;}
export async function loadMessages(threadId:string){const{data,error}=await supabase.from('chat_messages').select('id,thread_id,sender_id,body,created_at').eq('thread_id',threadId).order('created_at',{ascending:true});if(error)throw error;return(data??[])as ChatMessage[];}
export async function markThreadRead(threadId:string){const{error}=await supabase.rpc('mark_chat_thread_read',{p_thread_id:threadId});if(error)throw mapRpcError(error);emitSykaEvent({type:'chat-read',threadId});}
export async function getUnreadChatCount(){const{data,error}=await supabase.rpc('get_my_unread_chat_count');if(error)throw error;return Number(data??0);}
export async function closeThread(threadId:string,rating?:number){const{data,error}=await supabase.rpc('close_chat_thread',{p_thread_id:threadId,p_rating:rating??null});if(error)throw mapRpcError(error);return data as ChatThread;}
export async function submitRating(threadId:string,rating:number){if(!Number.isInteger(rating)||rating<1||rating>5)throw new Error('Rating harus 1-5.');await closeThread(threadId,rating);}
export async function createTicketThread(subject:string,description:string){const s=subject.trim(),d=description.trim();if(!s)throw new Error('Subjek wajib diisi.');if(!d)throw new Error('Deskripsi wajib diisi.');const{data,error}=await supabase.rpc('create_support_ticket',{p_subject:s,p_description:d});if(error)throw mapRpcError(error);return data as ChatThread;}
export async function getOrCreateThread(){const{data,error}=await supabase.rpc('get_or_create_support_thread');if(error)throw mapRpcError(error);return data as ChatThread;}
export const loadMyThread=async():Promise<ChatThread|null>=>{const threads=await loadMyThreads();return threads.find(t=>t.thread_type==='ticket'&&t.status==='open')??null;};export const loadMyMessages=loadMessages;
export async function searchUsersForChat(query:string,limit=10){const q=query.trim();if(!q)return[];const{data:auth}=await supabase.auth.getUser();if(!auth.user)return[];const safe=q.replace(/[%_,]/g,' ');const{data,error}=await supabase.from('profiles').select('id,username,full_name,avatar_url').or(`full_name.ilike.%${safe}%,username.ilike.%${safe}%`).neq('id',auth.user.id).limit(limit);if(error)throw error;return(data??[])as any[];}
export async function createDmFromProfile(targetUserId:string){return getOrCreateDmThread(targetUserId);}
export async function getFollowStatus(followerId:string,followingId:string){const{data,error}=await supabase.rpc('get_follow_status',{p_follower_id:followerId,p_following_id:followingId});if(error)throw error;return(data as FollowStatus)??'none';}
export async function requestFollow(targetUserId:string){const{data,error}=await supabase.rpc('create_follow_request',{p_target_user_id:targetUserId});if(error)throw mapRpcError(error);emitSykaEvent({type:'follow-updated',userId:targetUserId,status:String((data as any)?.status??'pending')});return data as any;}
export async function respondFollowRequest(followerId:string,accept:boolean){const{data,error}=await supabase.rpc('respond_follow_request',{p_follower_id:followerId,p_accept:accept});if(error)throw mapRpcError(error);emitSykaEvent({type:'follow-updated',userId:followerId,status:accept?'approved':'none'});return data;}
export async function removeFollow(targetUserId:string){const{error}=await supabase.rpc('remove_follow',{p_target_user_id:targetUserId});if(error)throw mapRpcError(error);emitSykaEvent({type:'follow-updated',userId:targetUserId,status:'none'});}
export async function isFollowApproved(followerId:string,followingId:string){const status=await getFollowStatus(followerId,followingId);return status==='approved'||status==='auto';}
export async function getUserPrivacy(userId:string){const{data,error}=await supabase.from('profiles').select('accept_messages').eq('id',userId).maybeSingle();if(error)throw error;return data?.accept_messages??'public';}
export async function blockChatUser(targetUserId:string){const{error}=await supabase.rpc('block_chat_user',{p_target_user_id:targetUserId});if(error)throw mapRpcError(error);emitSykaEvent({type:'chat-blocked',userId:targetUserId});}
export async function unblockChatUser(targetUserId:string){const{error}=await supabase.rpc('unblock_chat_user',{p_target_user_id:targetUserId});if(error)throw mapRpcError(error);emitSykaEvent({type:'chat-unblocked',userId:targetUserId});}
export async function isChatBlocked(targetUserId:string){const{data:auth}=await supabase.auth.getUser();if(!auth.user)return false;const{data,error}=await supabase.rpc('is_chat_blocked',{p_a:auth.user.id,p_b:targetUserId});if(error)throw mapRpcError(error);return Boolean(data);}
export async function hideChatThread(threadId:string){const{error}=await supabase.rpc('hide_chat_thread',{p_thread_id:threadId});if(error)throw mapRpcError(error);emitSykaEvent({type:'chat-hidden',threadId});}
export async function canDmUser(currentUserId:string,targetUserId:string){return isFollowApproved(currentUserId,targetUserId)&&!(await isChatBlocked(targetUserId));}
export async function acceptFollow(followerId:string,_followingId:string){await respondFollowRequest(followerId,true);}export async function rejectFollow(followerId:string,_followingId:string){await respondFollowRequest(followerId,false);}
